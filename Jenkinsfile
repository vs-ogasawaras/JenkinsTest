pipeline {
    agent any
    stages {
        stage('PWD') {
            steps {
                sh 'pwd'
            }
        }
        stage('LS') {
            steps {
                sh 'ls -l'
            }
        }
    }
}